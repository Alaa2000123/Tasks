@using API.Models;
@model Employee

@using (Html.BeginForm("Save","Home",FormMethod.Post))
{
<div>
    <label>Name : </label>
     @Html.TextBoxFor(e=>e.Name)
</div>
<input type="submit" value="Save"/>
}